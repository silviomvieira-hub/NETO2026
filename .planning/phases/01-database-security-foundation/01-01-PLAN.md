---
phase: 01-database-security-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - css/styles.css
  - js/constants.js
  - js/utils.js
  - js/router.js
  - js/modules/dashboard.js
  - js/modules/cidades.js
  - js/modules/agenda.js
  - js/modules/pessoas.js
  - js/modules/lideres.js
  - js/modules/quemequem.js
  - js/modules/demandas.js
  - js/modules/pesquisas.js
  - js/modules/materiais.js
  - js/modules/gamificacao.js
autonomous: true
requirements:
  - INFRA-05

must_haves:
  truths:
    - "Abrir index.html no navegador mostra a mesma interface visual identica ao prototipo original"
    - "Todas as 10 paginas (dashboard, cidades, agenda, pessoas, lideres, quemequem, demandas, pesquisas, materiais, gamificacao) navegam e renderizam corretamente"
    - "CSS variables, animacoes, dark theme estao 100% preservados"
    - "Codigo esta separado em arquivos ES6 module distintos"
  artifacts:
    - path: "css/styles.css"
      provides: "Todo o CSS extraido do index.html (linhas 8-1035)"
      contains: ":root"
    - path: "js/constants.js"
      provides: "REGIOES_DF, PAGE_TITLES, TROFEUS_DEF, STORAGE_KEY e outras constantes"
      exports: ["REGIOES_DF", "PAGE_TITLES", "STORAGE_KEY"]
    - path: "js/utils.js"
      provides: "Funcoes utilitarias: escapeHtml, formatDate, timeAgo, openModal, closeModal, triggerConfetti, gerarId"
      exports: ["escapeHtml", "formatDate", "timeAgo", "openModal", "closeModal"]
    - path: "js/router.js"
      provides: "showPage, renderPage, toggleSidebar - navegacao SPA"
      exports: ["showPage", "renderPage"]
    - path: "js/modules/gamificacao.js"
      provides: "renderGamificacao, getLevel, saveXP, updateStreak, checkTrofeus e toda logica de XP"
      exports: ["renderGamificacao"]
    - path: "index.html"
      provides: "Shell HTML minimo com link para CSS e script type=module para app.js"
      contains: "script type=\"module\""
  key_links:
    - from: "index.html"
      to: "js/app.js"
      via: "script type=module src"
      pattern: "<script type=\"module\" src=\"/js/app.js\">"
    - from: "js/router.js"
      to: "js/modules/*.js"
      via: "dynamic import das funcoes render de cada pagina"
      pattern: "import.*from.*modules/"
---

<objective>
Modularizar o prototipo monolitico index.html (2.474 linhas) em modulos ES6 separados, preservando 100% da funcionalidade e visual existentes.

Purpose: Criar a estrutura de arquivos que todas as fases seguintes utilizam. Sem modularizacao, nao e possivel adicionar Supabase, auth ou data layer de forma limpa.
Output: 17 arquivos (1 HTML shell, 1 CSS, 5 JS core, 10 JS modules) que reproduzem exatamente o comportamento do prototipo original.
</objective>

<execution_context>
@C:\Users\VAIO\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\VAIO\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-database-security-foundation/01-CONTEXT.md
@.planning/phases/01-database-security-foundation/01-RESEARCH.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extrair CSS, HTML shell, constantes e utilitarios</name>
  <files>index.html, css/styles.css, js/constants.js, js/utils.js</files>
  <read_first>
    - index.html (COMPLETO — este e o arquivo fonte de tudo, ler inteiro para extrair com fidelidade)
  </read_first>
  <action>
**1. Criar `css/styles.css`:**
Extrair TODO o conteudo entre `<style>` e `</style>` do index.html (linhas 8-1035) para `css/styles.css`. Incluir TODAS as CSS variables em `:root`, TODAS as animacoes (@keyframes fadeIn, pulse, glow, slideRight, float, spin, confetti, gradient, neonPulse, confettiFall), TODOS os estilos de componentes (sidebar, header, main-content, cards, modals, forms, buttons, stats-grid, etc.), e TODOS os media queries responsivos. Adicionar tambem o import do Google Fonts que esta no head:
```css
@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800;900&display=swap');
```
Tambem adicionar estilos para a pagina de login (serao usados no Plan 03):
```css
/* ===== LOGIN PAGE ===== */
.login-container {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--bg);
    padding: 20px;
}
.login-card {
    background: var(--bg-card);
    border: 1px solid rgba(0,230,118,0.15);
    border-radius: var(--radius);
    padding: 40px 32px;
    max-width: 400px;
    width: 100%;
    text-align: center;
    animation: fadeIn 0.6s ease-out;
}
.login-card img {
    width: 100px;
    height: 100px;
    border-radius: 50%;
    border: 3px solid var(--green);
    margin-bottom: 16px;
    object-fit: cover;
    animation: glow 3s ease-in-out infinite;
}
.login-card h1 {
    color: var(--green);
    font-size: 1.8rem;
    font-weight: 900;
    margin-bottom: 4px;
}
.login-card p {
    color: var(--text-dim);
    font-size: 0.9rem;
    margin-bottom: 24px;
}
.login-card form {
    display: flex;
    flex-direction: column;
    gap: 12px;
}
.login-card input {
    padding: 12px 16px;
    border: 2px solid rgba(255,255,255,0.1);
    border-radius: 10px;
    font-size: 0.95rem;
    background: var(--bg);
    color: var(--text);
    font-family: inherit;
    transition: border-color 0.3s;
}
.login-card input:focus {
    outline: none;
    border-color: var(--green);
}
.login-card button[type="submit"] {
    padding: 12px;
    background: linear-gradient(135deg, var(--green), var(--green-dark));
    color: #000;
    border: none;
    border-radius: 10px;
    font-size: 1rem;
    font-weight: 700;
    cursor: pointer;
    font-family: inherit;
    transition: transform 0.2s, box-shadow 0.2s;
    margin-top: 8px;
}
.login-card button[type="submit"]:hover {
    transform: scale(1.02);
    box-shadow: 0 0 20px var(--green-glow);
}
.error-message {
    color: var(--red);
    font-size: 0.85rem;
    margin-top: 12px;
    min-height: 20px;
}
.migration-banner {
    background: linear-gradient(135deg, rgba(255,214,0,0.15), rgba(255,214,0,0.05));
    border: 1px solid var(--gold);
    border-radius: var(--radius);
    padding: 16px 20px;
    margin-bottom: 20px;
    display: flex;
    align-items: center;
    gap: 12px;
    animation: fadeIn 0.6s ease-out;
}
.migration-banner .btn {
    white-space: nowrap;
}
```

**2. Criar `js/constants.js`:**
```javascript
// js/constants.js — Constantes extraidas do prototipo original
export const STORAGE_KEY = 'neto_campanha_2026';

export const REGIOES_DF = [
    'Plano Piloto','Gama','Taguatinga','Brazlândia','Sobradinho',
    'Planaltina','Paranoá','Núcleo Bandeirante','Ceilândia','Guará',
    'Cruzeiro','Samambaia','Santa Maria','São Sebastião','Recanto das Emas',
    'Lago Sul','Riacho Fundo','Lago Norte','Candangolândia','Águas Claras',
    'Riacho Fundo II','Sudoeste/Octogonal','Varjão','Park Way',
    'SCIA/Estrutural','Sobradinho II','Jardim Botânico','Itapoã',
    'SIA','Vicente Pires','Fercal','Sol Nascente/Pôr do Sol','Arniqueira'
];

export const PAGE_TITLES = {
    dashboard:'🚀 Dashboard', cidades:'🗺️ Cidades Visitadas',
    agenda:'📅 Agenda do Neto', pessoas:'👥 Time do Neto',
    lideres:'⭐ Líderes', quemequem:'🔍 Quem é Quem',
    demandas:'📋 Demandas', pesquisas:'📊 Pesquisas',
    materiais:'📦 Materiais', gamificacao:'🏆 Gamificação'
};

export const TIPOS_EVENTO = ['Reunião','Visita','Evento','Debate','Caminhada','Live','Outro'];
export const FUNCOES_PESSOA = ['coordenador','apoiador','voluntário','liderança','assessor','digital','motorista'];
export const TIPOS_LIDER = ['comunitária','religiosa','sindical','estudantil','empresarial','política','digital','social'];
export const RELACOES_POLITICAS = ['aliado','neutro','adversário','potencial aliado'];
export const CATEGORIAS_DEMANDA = ['infraestrutura','saúde','educação','segurança','transporte','social','meio ambiente'];
export const PRIORIDADES_DEMANDA = ['alta','média','baixa'];

// XP Action types com pontuacao (extraidas do prototipo)
export const XP_ACOES = [
    { value: 'Cadastrou apoiador em campo', xp: 20 },
    { value: 'Visitou cidade/RA', xp: 30 },
    { value: 'Distribuiu material', xp: 15 },
    { value: 'Participou de evento', xp: 25 },
    { value: 'Organizou reunião', xp: 35 },
    { value: 'Fez live/vídeo', xp: 40 },
    { value: 'Postou em rede social', xp: 10 },
    { value: 'Cadastrou líder', xp: 30 },
    { value: 'Mapeou demanda', xp: 20 },
    { value: 'Acompanhou pesquisa', xp: 15 },
    { value: 'Fez articulação política', xp: 35 },
    { value: 'Trouxe doação/recurso', xp: 50 },
    { value: 'Capacitou equipe', xp: 30 },
    { value: 'Ação especial', xp: 25 }
];

// Trofeus definition
export const TROFEUS_DEF = [
    { id: 'primeiro_xp', nome: 'Primeiro Passo', icon: '🎯', desc: 'Ganhou XP pela primeira vez', check: (p, rk) => rk.acoes >= 1 },
    { id: 'xp100', nome: 'Centurião', icon: '💯', desc: 'Acumulou 100 XP', check: (p, rk) => rk.xp >= 100 },
    { id: 'xp500', nome: 'Meio Milhar', icon: '🔥', desc: 'Acumulou 500 XP', check: (p, rk) => rk.xp >= 500 },
    { id: 'xp1000', nome: 'Milionário de XP', icon: '💎', desc: 'Acumulou 1000 XP', check: (p, rk) => rk.xp >= 1000 },
    { id: 'streak3', nome: 'Consistente', icon: '📅', desc: '3 dias seguidos ativo', check: (p, rk) => false },
    { id: 'streak7', nome: 'Maratonista', icon: '🏃', desc: '7 dias seguidos ativo', check: (p, rk) => false },
    { id: 'streak15', nome: 'Imparável', icon: '⚡', desc: '15 dias seguidos ativo', check: (p, rk) => false },
    { id: 'acoes10', nome: 'Trabalhador', icon: '💪', desc: '10 ações registradas', check: (p, rk) => rk.acoes >= 10 },
    { id: 'acoes50', nome: 'Incansável', icon: '🦾', desc: '50 ações registradas', check: (p, rk) => rk.acoes >= 50 },
    { id: 'diversidade', nome: 'Canivete Suíço', icon: '🛠️', desc: 'Realizou 5 tipos diferentes de ação', check: (p, rk) => false },
    { id: 'primeiro_lugar', nome: 'Número 1', icon: '👑', desc: 'Ficou em 1º no ranking', check: (p, rk) => false },
    { id: 'missao_completa', nome: 'Missão Cumprida', icon: '🎖️', desc: 'Completou uma missão', check: (p, rk) => false }
];
```
NOTA: Os checks com `false` sao para trofeus que dependem de contexto externo (streaks, ranking). A logica completa de verificacao fica em gamificacao.js. Copiar EXATAMENTE os checks do prototipo original para os que usam `rk.acoes` e `rk.xp`.

**3. Criar `js/utils.js`:**
```javascript
// js/utils.js — Funcoes utilitarias extraidas do prototipo
export function escapeHtml(s) {
    return s ? String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;') : '';
}

export function formatDate(d) {
    if(!d) return '-';
    const p = d.split('-');
    return p.length === 3 ? p[2]+'/'+p[1]+'/'+p[0] : d;
}

export function timeAgo(iso) {
    if(!iso) return '';
    const m = Math.floor((Date.now() - new Date(iso).getTime()) / 60000);
    if(m < 1) return 'agora';
    if(m < 60) return m + 'min';
    const h = Math.floor(m / 60);
    if(h < 24) return h + 'h';
    return Math.floor(h / 24) + 'd';
}

export function openModal(id) {
    document.getElementById(id).classList.add('show');
}

export function closeModal(id) {
    document.getElementById(id).classList.remove('show');
}

export function triggerConfetti() {
    const colors = ['#00e676','#ffd600','#448aff','#ff4081','#b388ff','#00e5ff','#ff9100'];
    for (let i = 0; i < 30; i++) {
        setTimeout(() => {
            const el = document.createElement('div');
            el.className = 'confetti-piece';
            el.style.left = Math.random() * 100 + 'vw';
            el.style.top = Math.random() * 30 + 'vh';
            el.style.background = colors[Math.floor(Math.random() * colors.length)];
            el.style.width = (Math.random() * 8 + 4) + 'px';
            el.style.height = (Math.random() * 8 + 4) + 'px';
            el.style.borderRadius = Math.random() > 0.5 ? '50%' : '2px';
            document.body.appendChild(el);
            setTimeout(() => el.remove(), 1600);
        }, i * 30);
    }
}

// Inicializar listener ESC para fechar modais
document.addEventListener('keydown', e => {
    if(e.key === 'Escape') {
        document.querySelectorAll('.modal-overlay.show').forEach(m => m.classList.remove('show'));
    }
});
```

**4. Reescrever `index.html` como shell minimo:**
Manter TODO o HTML do body (sidebar, header, main com todas as 10 pages e modais) EXATAMENTE como esta. Remover:
- Todo o conteudo `<style>...</style>` (substituido pelo link CSS)
- Todo o conteudo `<script>...</script>` (substituido pelo module import)

O novo index.html tera:
```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NETO RODRIGUES - Pré-Campanha 2026</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <!-- TODO O HTML DO BODY PERMANECE IDENTICO -->
    <!-- sidebar, header, main-content com as 10 pages, todos os modais -->
    <!-- ... (copiar linhas 1038-1600 do original exatamente) ... -->

    <script type="module" src="/js/app.js"></script>
</body>
</html>
```
IMPORTANTE: Remover os `onclick="showPage('...')"` inline do HTML e substituir por `data-page` attributes. Os event listeners serao registrados pelo router.js. Exemplo:
- `onclick="showPage('dashboard')"` vira `data-page="dashboard"`
- `onclick="openModal('modalEvento')"` vira `data-modal="modalEvento"`
- `onclick="exportData()"` vira `id="btnExport"`
- `onclick="triggerImport()"` vira `id="btnImport"`
- `onclick="toggleSidebar()"` permanece via id do botao

ALTERNATIVA PRAGMATICA: Se a substituicao de inline handlers causar muita complexidade, manter os handlers inline e expor as funcoes como globals no app.js via `window.showPage = showPage`, etc. Isso e aceitavel em Phase 1 para preservar compatibilidade — refactor para event delegation pode vir depois.
  </action>
  <acceptance_criteria>
    - css/styles.css existe e contem `:root {` com todas as CSS variables (--bg, --green, --gold, etc.)
    - css/styles.css contem `@keyframes fadeIn` e `@keyframes confettiFall`
    - css/styles.css contem `@media (max-width: 900px)`
    - css/styles.css contem `.login-container` e `.login-card`
    - js/constants.js contem `export const REGIOES_DF` com 33 items no array
    - js/constants.js contem `export const PAGE_TITLES`
    - js/constants.js contem `export const TROFEUS_DEF`
    - js/constants.js contem `export const STORAGE_KEY`
    - js/utils.js contem `export function escapeHtml`
    - js/utils.js contem `export function formatDate`
    - js/utils.js contem `export function openModal`
    - js/utils.js contem `export function triggerConfetti`
    - index.html contem `<link rel="stylesheet" href` apontando para styles.css
    - index.html contem `<script type="module" src` apontando para app.js
    - index.html NAO contem tag `<style>`
    - index.html mantem todo o HTML do body (sidebar, header, main-content, modais)
  </acceptance_criteria>
  <verify>
    <automated>grep -c ":root {" css/styles.css && grep -c "export const REGIOES_DF" js/constants.js && grep -c "export function escapeHtml" js/utils.js && grep -c "script type=\"module\"" index.html && grep -cL "<style>" index.html</automated>
  </verify>
  <done>CSS extraido, constants e utils como ES6 modules, index.html e shell HTML limpo com module import</done>
</task>

<task type="auto">
  <name>Task 2: Extrair router, app.js e 10 modulos de pagina</name>
  <files>js/app.js, js/router.js, js/modules/dashboard.js, js/modules/cidades.js, js/modules/agenda.js, js/modules/pessoas.js, js/modules/lideres.js, js/modules/quemequem.js, js/modules/demandas.js, js/modules/pesquisas.js, js/modules/materiais.js, js/modules/gamificacao.js</files>
  <read_first>
    - index.html (para extrair TODA a logica JS — linhas 1603-2474)
    - js/constants.js (criado na Task 1 — para saber o que ja foi extraido e o que importar)
    - js/utils.js (criado na Task 1 — para saber o que ja foi extraido e o que importar)
  </read_first>
  <action>
**1. Criar `js/router.js`:**
Extrair a logica de navegacao SPA do prototipo (linhas 1682-1706). O router importa os render functions de cada modulo de pagina.

```javascript
// js/router.js
import { PAGE_TITLES } from './constants.js';
// Lazy imports dos modulos de pagina
import { renderDashboard } from './modules/dashboard.js';
import { renderCidades } from './modules/cidades.js';
import { renderAgenda } from './modules/agenda.js';
import { renderPessoas } from './modules/pessoas.js';
import { renderLideres } from './modules/lideres.js';
import { renderQuemEQuem } from './modules/quemequem.js';
import { renderDemandas } from './modules/demandas.js';
import { renderPesquisas } from './modules/pesquisas.js';
import { renderMateriais } from './modules/materiais.js';
import { renderGamificacao } from './modules/gamificacao.js';

const PAGE_RENDERERS = {
    dashboard: renderDashboard,
    cidades: renderCidades,
    agenda: renderAgenda,
    pessoas: renderPessoas,
    lideres: renderLideres,
    quemequem: renderQuemEQuem,
    demandas: renderDemandas,
    pesquisas: renderPesquisas,
    materiais: renderMateriais,
    gamificacao: renderGamificacao
};

export function showPage(page) {
    document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
    document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
    const el = document.getElementById('page-' + page);
    if (el) el.classList.add('active');
    document.querySelectorAll('.nav-item').forEach(n => {
        if (n.getAttribute('data-page') === page) n.classList.add('active');
    });
    const headerTitle = document.getElementById('headerTitle');
    if (headerTitle) headerTitle.textContent = PAGE_TITLES[page] || page;
    const sidebar = document.getElementById('sidebar');
    if (sidebar) sidebar.classList.remove('open');
    renderPage(page);
}

export function renderPage(page) {
    if (PAGE_RENDERERS[page]) PAGE_RENDERERS[page]();
}

export function toggleSidebar() {
    document.getElementById('sidebar').classList.toggle('open');
}
```

**2. Criar cada modulo de pagina em `js/modules/`:**
Cada arquivo exporta a funcao `render<Page>()` correspondente. A logica interna de cada render funcao permanece IDENTICA ao prototipo — apenas muda de funcao global para funcao exportada. Cada modulo importa o que precisa de constants.js, utils.js e do data store.

Para Phase 1, os modulos AINDA usam localStorage como data source (o data layer Supabase vem no Plan 04). Criar um modulo temporario de data store:

```javascript
// js/store.js — Data store temporario (localStorage, sera substituido por data.js no Plan 04)
import { STORAGE_KEY } from './constants.js';

function defaultData() {
    return {
        eventos: [], pessoas: [], lideres: [], quemequem: [],
        demandas: [], pesquisas: [], materiais: [], gamificacao: [],
        visitas: {}, atividades: [], nextId: 1,
        tarefas_game: [], streaks: {}, trofeus: []
    };
}

export let data = loadData();

function loadData() {
    try {
        const raw = localStorage.getItem(STORAGE_KEY);
        if (raw) {
            const parsed = JSON.parse(raw);
            const def = defaultData();
            for (const k in def) { if (parsed[k] === undefined) parsed[k] = def[k]; }
            return parsed;
        }
    } catch(e) { console.error('Erro ao carregar:', e); }
    return defaultData();
}

export function saveData() {
    try { localStorage.setItem(STORAGE_KEY, JSON.stringify(data)); }
    catch(e) { console.error('Erro ao salvar:', e); }
}

export function gerarId() { return data.nextId++; }

export function addAtividade(texto) {
    data.atividades.unshift({ texto, data: new Date().toISOString() });
    if (data.atividades.length > 50) data.atividades.length = 50;
}

export function exportData() {
    const blob = new Blob([JSON.stringify(data, null, 2)], { type: 'application/json' });
    const a = document.createElement('a');
    a.href = URL.createObjectURL(blob);
    a.download = 'neto-backup-' + new Date().toISOString().split('T')[0] + '.json';
    a.click();
    URL.revokeObjectURL(a.href);
}

export function handleImport(importedData) {
    const def = defaultData();
    for (const k in def) { if (importedData[k] === undefined) importedData[k] = def[k]; }
    Object.assign(data, importedData);
    saveData();
}
```

Cada modulo de pagina (10 arquivos) segue este padrao — exemplo para dashboard.js:
```javascript
// js/modules/dashboard.js
import { data } from '../store.js';
import { escapeHtml, formatDate, timeAgo } from '../utils.js';

export function renderDashboard() {
    // COPIAR EXATAMENTE a logica de renderDashboard() do prototipo (linhas correspondentes)
    // Substituir referencia global `data` por import
    // Manter TODA a logica de stats, countdown, proximos eventos, atividades recentes
    updateCountdown();
    // ... resto da logica identica
}

function updateCountdown() {
    const d = Math.ceil((new Date('2026-10-04') - new Date()) / 86400000);
    const el = document.getElementById('countdownDias');
    if (el) el.textContent = d > 0 ? d : 0;
}
```

Para CADA um dos 10 modulos: extrair a funcao render correspondente do prototipo, importar dependencias de constants/utils/store, e exportar a funcao render. NAO alterar a logica interna — apenas reorganizar imports/exports.

Modulos e suas funcoes principais:
- `dashboard.js`: renderDashboard(), updateCountdown(), updateClock()
- `cidades.js`: renderCidades() — usa REGIOES_DF
- `agenda.js`: renderAgenda(), salvarEvento(), excluirEvento() — usa TIPOS_EVENTO
- `pessoas.js`: renderPessoas(), salvarPessoa(), excluirPessoa() — usa FUNCOES_PESSOA, REGIOES_DF
- `lideres.js`: renderLideres(), salvarLider(), excluirLider() — usa TIPOS_LIDER, REGIOES_DF
- `quemequem.js`: renderQuemEQuem(), salvarQuem(), excluirQuem() — usa RELACOES_POLITICAS
- `demandas.js`: renderDemandas(), salvarDemanda(), resolverDemanda(), excluirDemanda() — usa CATEGORIAS_DEMANDA, PRIORIDADES_DEMANDA
- `pesquisas.js`: renderPesquisas(), salvarPesquisa(), excluirPesquisa()
- `materiais.js`: renderMateriais(), salvarMaterial(), excluirMaterial()
- `gamificacao.js`: renderGamificacao(), getLevel(), saveXP(), updateStreak(), checkTrofeus(), renderTabela(), renderRanking() — usa TROFEUS_DEF, XP_ACOES

**3. Criar `js/app.js` (entry point):**
```javascript
// js/app.js — Entry point da aplicacao
import { showPage, toggleSidebar } from './router.js';
import { exportData, handleImport } from './store.js';
import { openModal, closeModal, triggerConfetti } from './utils.js';

// Expor funcoes como globais para onclick handlers no HTML
// (pragmatico para Phase 1 — event delegation vem em fase futura)
window.showPage = showPage;
window.toggleSidebar = toggleSidebar;
window.exportData = exportData;
window.openModal = openModal;
window.closeModal = closeModal;
window.triggerConfetti = triggerConfetti;

// Import handler
window.triggerImport = () => document.getElementById('importInput').click();
window.handleImport = (e) => {
    const f = e.target.files[0];
    if (!f) return;
    const r = new FileReader();
    r.onload = function(ev) {
        try {
            const imp = JSON.parse(ev.target.result);
            if (confirm('Substituir todos os dados pelo backup?')) {
                handleImport(imp);
                showPage('dashboard');
                alert('Dados importados!');
                triggerConfetti();
            }
        } catch(err) { alert('Arquivo inválido: ' + err.message); }
    };
    r.readAsText(f);
    e.target.value = '';
};

// Expor funcoes de cada modulo que sao chamadas via onclick no HTML
// Importar salvar/excluir de cada modulo e expor como global
import { salvarEvento, excluirEvento } from './modules/agenda.js';
import { salvarPessoa, excluirPessoa } from './modules/pessoas.js';
import { salvarLider, excluirLider } from './modules/lideres.js';
import { salvarQuem, excluirQuem } from './modules/quemequem.js';
import { salvarDemanda, resolverDemanda, excluirDemanda } from './modules/demandas.js';
import { salvarPesquisa, excluirPesquisa } from './modules/pesquisas.js';
import { salvarMaterial, excluirMaterial } from './modules/materiais.js';
import { saveXP } from './modules/gamificacao.js';
import { registrarVisita } from './modules/cidades.js';

window.salvarEvento = salvarEvento;
window.excluirEvento = excluirEvento;
window.salvarPessoa = salvarPessoa;
window.excluirPessoa = excluirPessoa;
window.salvarLider = salvarLider;
window.excluirLider = excluirLider;
window.salvarQuem = salvarQuem;
window.excluirQuem = excluirQuem;
window.salvarDemanda = salvarDemanda;
window.resolverDemanda = resolverDemanda;
window.excluirDemanda = excluirDemanda;
window.salvarPesquisa = salvarPesquisa;
window.excluirPesquisa = excluirPesquisa;
window.salvarMaterial = salvarMaterial;
window.excluirMaterial = excluirMaterial;
window.saveXP = saveXP;
window.registrarVisita = registrarVisita;

// Inicializar
function init() {
    showPage('dashboard');
    // Clock update
    function updateClock() {
        const el = document.getElementById('headerClock');
        if (el) el.textContent = new Date().toLocaleDateString('pt-BR', {
            weekday:'long', day:'numeric', month:'long', hour:'2-digit', minute:'2-digit'
        });
    }
    setInterval(updateClock, 1000);
    updateClock();

    // Keyboard nav
    document.querySelectorAll('.nav-item').forEach(i => {
        i.addEventListener('keydown', e => {
            if(e.key === 'Enter' || e.key === ' ') { e.preventDefault(); i.click(); }
        });
    });
}

init();
```

NOTA CRITICA: O executor DEVE ler index.html inteiro (2474 linhas) para extrair com exatidao cada funcao render e suas subfuncoes. Os exemplos acima sao esqueleticos — o conteudo real vem do prototipo. Cada funcao como renderDashboard(), renderAgenda(), etc. deve ser copiada INTEGRALMENTE do prototipo, apenas adaptando `data` global para import do store.js.
  </action>
  <acceptance_criteria>
    - js/app.js existe e contem `import { showPage` de router.js
    - js/app.js contem `window.showPage = showPage`
    - js/app.js contem chamada `init()` ou `showPage('dashboard')`
    - js/router.js contem `export function showPage`
    - js/router.js contem `export function renderPage`
    - js/router.js importa renderDashboard de './modules/dashboard.js'
    - js/store.js existe e contem `export let data` e `export function saveData`
    - js/store.js contem `export function gerarId`
    - js/store.js contem `export function addAtividade`
    - js/modules/dashboard.js contem `export function renderDashboard`
    - js/modules/cidades.js contem `export function renderCidades`
    - js/modules/agenda.js contem `export function renderAgenda`
    - js/modules/agenda.js contem `export function salvarEvento`
    - js/modules/pessoas.js contem `export function renderPessoas`
    - js/modules/lideres.js contem `export function renderLideres`
    - js/modules/quemequem.js contem `export function renderQuemEQuem`
    - js/modules/demandas.js contem `export function renderDemandas`
    - js/modules/pesquisas.js contem `export function renderPesquisas`
    - js/modules/materiais.js contem `export function renderMateriais`
    - js/modules/gamificacao.js contem `export function renderGamificacao`
    - js/modules/gamificacao.js contem `export function saveXP`
    - js/modules/gamificacao.js contem `function getLevel`
    - Todos os 10 modulos importam de '../store.js' e '../utils.js' ou '../constants.js'
    - Abrir index.html em servidor local mostra o app funcionando (todas as paginas navegam e renderizam)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "export function showPage" js/router.js && grep -c "export function renderDashboard" js/modules/dashboard.js && grep -c "export function renderGamificacao" js/modules/gamificacao.js && ls js/modules/ | wc -l</automated>
  </verify>
  <done>12 arquivos JS criados (app.js, router.js, store.js, 10 modulos). App funciona identicamente ao prototipo original abrindo index.html com um servidor local (ex: npx serve ou python -m http.server). Todas as 10 paginas navegam, dados persistem em localStorage, gamificacao funciona.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage | Dados do usuario armazenados localmente (sem autenticacao neste plano) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | localStorage data | accept | Fase 1 Plan 01 apenas modulariza codigo — mesma superficie de ataque do prototipo original. Autenticacao vem no Plan 03. |
| T-01-02 | Information Disclosure | Credenciais hardcoded | mitigate | Nenhuma credencial introduzida neste plano. Supabase client vem no Plan 02/03 com .env. |
| T-01-03 | Tampering | XSS via escapeHtml | mitigate | escapeHtml() preservada identica do prototipo — sanitiza &, <, >, ". Validar que todos os modulos usam escapeHtml antes de innerHTML. |
</threat_model>

<verification>
1. Abrir index.html com servidor local (`npx serve .` ou `python -m http.server`)
2. Verificar que todas as 10 paginas carregam e renderizam
3. Verificar que CRUD funciona (criar evento, pessoa, demanda)
4. Verificar que gamificacao funciona (registrar XP, ver ranking)
5. Verificar que export/import JSON funciona
6. Network tab do DevTools mostra carregamento dos modulos ES6
7. Console do DevTools nao mostra erros
</verification>

<success_criteria>
- App modularizado em 17+ arquivos mantem funcionalidade identica ao prototipo de 2474 linhas
- Zero regressao visual (dark theme, animacoes, responsividade preservados)
- Todos os modulos usam ES6 import/export
- localStorage continua funcionando como data store temporario (per D-01, D-02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-security-foundation/01-01-SUMMARY.md`
</output>
